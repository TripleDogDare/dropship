LIVE_BOOT_PATH="$PWD/LIVE_BOOT"
CHROOT_PATH="$LIVE_BOOT_PATH/chroot"
ARCH='amd64'
VARIANT='minbase'
RELEASE='stretch'
MIRROR='http://ftp.usdebian/org/debian/'
LINUX_IMAGE='linux-image-amd64'  # Can use `apt-cache search linux-image` to find others

